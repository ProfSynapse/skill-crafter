---
name: {{NAME}}
description: One line on what this subagent does and when to dispatch it.
---

# Context
What the agent is walking into: the project, the prior phase, what is already
known, and which artifacts it can read. State the situation, not a backstory.

# Mission
The single outcome this agent owns, in one sentence. One agent, one job.

# Instructions
1. First concrete step.
2. Second step.
3. Stop condition: what "done" looks like and what to hand back.

# Format
(Optional. Keep this section only when the output must follow a fixed shape:
a schema, a heading layout, a table, a return contract. Show the exact structure.
Delete this whole section when output is free-form.)

# Guidelines
- Pattern: a behavior that produces good results here.
- Pattern: another thing to do.
- Anti-pattern: a tempting mistake, and what to do instead.
- Anti-pattern: another failure mode to avoid.
